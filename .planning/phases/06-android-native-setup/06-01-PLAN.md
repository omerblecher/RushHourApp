---
phase: 06-android-native-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - android/variables.gradle
  - android/app/src/main/AndroidManifest.xml
  - android/app/src/main/res/values/strings.xml
  - .env.development
  - .env.production
autonomous: true
requirements:
  - SETUP-01
  - SETUP-03
  - SETUP-02
  - SETUP-04
  - SETUP-05

must_haves:
  truths:
    - "npm has @capacitor-community/admob@8.0.0 installed as a dependency"
    - "Gradle pins play-services-ads to 24.3.0 via variables.gradle"
    - "AndroidManifest.xml declares the AdMob App ID meta-data and AD_ID permission"
    - "strings.xml contains the Google test App ID under key admob_app_id"
    - ".env.development contains Google test ad unit IDs for app/banner/interstitial"
    - ".env.production exists as a committed TODO placeholder for Phase 10"
    - "npx cap sync registers @capacitor-community/admob (8.0.0) as an Android plugin"
  artifacts:
    - path: "package.json"
      provides: "AdMob plugin dependency at pinned version"
      contains: '"@capacitor-community/admob": "8.0.0"'
    - path: "android/variables.gradle"
      provides: "playServicesAdsVersion pin preventing Firebase/AdMob merge conflict"
      contains: "playServicesAdsVersion = '24.3.0'"
    - path: "android/app/src/main/AndroidManifest.xml"
      provides: "AdMob App ID meta-data and AD_ID runtime permission"
      contains: "com.google.android.gms.ads.APPLICATION_ID"
    - path: "android/app/src/main/res/values/strings.xml"
      provides: "admob_app_id string resource (test App ID)"
      contains: "ca-app-pub-3940256099942544~3347511713"
    - path: ".env.development"
      provides: "Vite dev mode AdMob test IDs (app, banner, interstitial)"
      contains: "VITE_ADMOB_APP_ID=ca-app-pub-3940256099942544~3347511713"
    - path: ".env.production"
      provides: "Vite prod mode AdMob IDs (Phase 10 fills real values)"
      contains: "VITE_ADMOB_APP_ID=TODO"
  key_links:
    - from: "android/app/src/main/AndroidManifest.xml"
      to: "android/app/src/main/res/values/strings.xml"
      via: "@string/admob_app_id reference"
      pattern: '@string/admob_app_id'
    - from: "android/variables.gradle"
      to: "node_modules/@capacitor-community/admob/android/build.gradle"
      via: "Gradle property override of playServicesAdsVersion"
      pattern: "playServicesAdsVersion"
    - from: "package.json"
      to: "android/capacitor.settings.gradle"
      via: "npx cap sync registers :capacitor-community-admob subproject"
      pattern: "capacitor-community-admob"
---

<objective>
Install the @capacitor-community/admob@8.0.0 plugin and wire up all Android native config + Vite env files so the app builds cleanly and launches without crash on Android. This is a purely native/config phase — zero TypeScript touched (per D-01).

Purpose: Establish the foundation for Phase 7 (adService.ts + UMP consent). Without the plugin registered, version pinned, App ID in the manifest, AD_ID permission declared, and env IDs available to Vite, no ad work in phases 7–10 can proceed.

Output: Plugin installed in package.json, gradle version pin in variables.gradle, manifest meta-data + permission, strings resource, two committed env files, cap sync run successfully.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-android-native-setup/06-CONTEXT.md
@.planning/phases/06-android-native-setup/06-RESEARCH.md
@android/variables.gradle
@android/app/src/main/AndroidManifest.xml
@android/app/src/main/res/values/strings.xml
@.gitignore

<interfaces>
<!-- Current project state — executor should not discover these -->

Current android/variables.gradle ends at line 16 with `cordovaAndroidVersion = '14.0.1'` inside `ext { }`. New line is appended before the closing brace.

Current AndroidManifest.xml structure:
- Line 3-31: <application> block containing MainActivity and FileProvider
- Line 35: <uses-permission android:name="android.permission.INTERNET" />
- Line 36: </manifest>
- No xmlns:tools declaration, no existing meta-data under <application> (only under FileProvider)

Current strings.xml resources:
- app_name = "Rush Hour"
- title_activity_main = "Rush Hour"
- package_name = "com.rushhour.puzzle"
- custom_url_scheme = "com.rushhour.puzzle"

Current .gitignore entries: node_modules/, dist/, *.tsbuildinfo, .env, .env.local, *.keystore, *.jks, android/app/google-services.json
  - .env.development and .env.production are NOT ignored — they will be tracked (intentional, test IDs are public)

Current package.json dependencies include @capacitor/core@8.2.0, @capacitor/android@8.2.0 — compatible with @capacitor-community/admob@8.0.0.

Google public test IDs (from CONTEXT.md D-02, D-03):
  App ID:          ca-app-pub-3940256099942544~3347511713
  Banner unit:     ca-app-pub-3940256099942544/6300978111
  Interstitial:    ca-app-pub-3940256099942544/1033173712
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install AdMob plugin, pin play-services-ads version, run cap sync</name>
  <files>package.json, package-lock.json, android/variables.gradle</files>
  <read_first>
    - package.json (confirm existing Capacitor 8 stack before adding dep)
    - android/variables.gradle (current content has 15 ext properties, no playServicesAdsVersion)
    - .planning/phases/06-android-native-setup/06-CONTEXT.md (locked decisions on plugin version + gradle pin)
  </read_first>
  <action>
    1. Install plugin at exact version (per RESEARCH Standard Stack):
       `npm install @capacitor-community/admob@8.0.0`
       This must add `"@capacitor-community/admob": "8.0.0"` (exact, no caret) to package.json dependencies. If npm adds `^8.0.0`, edit package.json manually to remove the caret so the version is pinned.

    2. Edit `android/variables.gradle`. Add this line inside the existing `ext { }` block, immediately after `cordovaAndroidVersion = '14.0.1'` and before the closing brace:
       ```groovy
           // AdMob: pin to avoid Firebase/AdMob manifest merge conflict (Phase 6, SETUP-03)
           playServicesAdsVersion = '24.3.0'
       ```
       The assignment must be exactly `playServicesAdsVersion = '24.3.0'` (single quotes, value `24.3.0`). This overrides the plugin's floating `24.9.+` default via Gradle's `project.hasProperty()` check.

    3. Run `npx cap sync` from the project root. The output must contain the line `@capacitor-community/admob (8.0.0)` under the "Found X Capacitor plugin for android:" section. If sync fails, DO NOT proceed — investigate before continuing.

    4. Verify `android/capacitor.settings.gradle` was auto-updated by cap sync to include the `:capacitor-community-admob` subproject.

    Notes:
    - Do NOT touch any .ts or .tsx file (D-01: purely native phase).
    - Do NOT add AdMob config to `capacitor.config.ts` — the plugin requires no config there (RESEARCH Pattern 4).
    - Do NOT use the plugin's default `24.9.+` — pin explicitly to `24.3.0` per D (locked decision from v1.1 Research).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');if(p.dependencies['@capacitor-community/admob']!=='8.0.0'){process.exit(1)}" && grep -q "playServicesAdsVersion = '24.3.0'" android/variables.gradle && grep -q "capacitor-community-admob" android/capacitor.settings.gradle && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies contain exactly `"@capacitor-community/admob": "8.0.0"` (no caret, no tilde)
    - `package-lock.json` exists and contains `@capacitor-community/admob` at `8.0.0`
    - `grep "playServicesAdsVersion = '24.3.0'" android/variables.gradle` returns a match
    - `grep "cordovaAndroidVersion" android/variables.gradle` still returns a match (existing line preserved)
    - `grep "capacitor-community-admob" android/capacitor.settings.gradle` returns a match (proves cap sync ran)
    - `android/variables.gradle` remains valid Groovy syntax (opening `ext {` and closing `}`, both present, balanced braces)
    - `npm test` exits 0 (no regression from install; existing Vitest suite still green)
  </acceptance_criteria>
  <done>Plugin installed at exact version 8.0.0, gradle pinned to 24.3.0, cap sync registered the Android subproject, and existing test suite remains green.</done>
</task>

<task type="auto">
  <name>Task 2: Add AdMob App ID meta-data, AD_ID permission, and strings.xml resource</name>
  <files>android/app/src/main/AndroidManifest.xml, android/app/src/main/res/values/strings.xml</files>
  <read_first>
    - android/app/src/main/AndroidManifest.xml (current state: no meta-data under application, single INTERNET permission, no xmlns:tools)
    - android/app/src/main/res/values/strings.xml (current state: 4 resources, no admob_app_id)
    - .planning/phases/06-android-native-setup/06-RESEARCH.md (Pattern 2 — exact manifest and strings layout)
    - .planning/phases/06-android-native-setup/06-CONTEXT.md (D-03 locks test App ID value)
  </read_first>
  <action>
    1. Edit `android/app/src/main/res/values/strings.xml`. Add this line inside the existing `<resources>` block, immediately before `</resources>`:
       ```xml
           <!-- AdMob test App ID (Phase 6, SETUP-02). Phase 10 replaces with real ID. -->
           <string name="admob_app_id">ca-app-pub-3940256099942544~3347511713</string>
       ```
       The string value must be exactly `ca-app-pub-3940256099942544~3347511713` (Google's public test App ID per D-03). This is NOT a real production ID and is safe to commit.

    2. Edit `android/app/src/main/AndroidManifest.xml`. Two changes:

       **Change A — Add meta-data under `<application>`**: Immediately after the opening `<application ...>` tag (after line 9, `android:theme="@style/AppTheme">`), insert:
       ```xml

               <!-- AdMob App ID meta-data (SETUP-02). Value lives in strings.xml for clean diffs. -->
               <meta-data
                   android:name="com.google.android.gms.ads.APPLICATION_ID"
                   android:value="@string/admob_app_id"/>
       ```
       The `android:name` must be exactly `com.google.android.gms.ads.APPLICATION_ID`. The `android:value` must be exactly `@string/admob_app_id` (reference, NOT the inline ID). This meta-data must be a direct child of `<application>`, NOT nested inside `<activity>` or `<provider>`.

       **Change B — Add AD_ID permission**: Immediately after the existing `<uses-permission android:name="android.permission.INTERNET" />` line, add:
       ```xml
           <!-- Required for Android 13+ (API 33+) advertising ID (SETUP-04). -->
           <uses-permission android:name="com.google.android.gms.permission.AD_ID"/>
       ```
       The permission name must be exactly `com.google.android.gms.permission.AD_ID`. It must be a direct child of `<manifest>`, alongside the INTERNET permission.

    3. Do NOT add `xmlns:tools` — the `tools:replace` fallback from RESEARCH Pitfall 3 is only applied if the Gradle build later fails with a manifest merge conflict. Phase 6 trusts the `24.3.0` pin to avoid that.

    Notes:
    - Do NOT inline the App ID in `android:value` — use the string resource reference per RESEARCH Pattern 2 (best practice, cleaner Phase 10 swap).
    - Do NOT put the meta-data inside `<activity>` — pitfall 2 in RESEARCH: crash with `IllegalStateException` if meta-data is under activity instead of application.
    - The existing `<meta-data>` inside `<provider>` for FILE_PROVIDER_PATHS stays untouched.
  </action>
  <verify>
    <automated>grep -q 'com.google.android.gms.ads.APPLICATION_ID' android/app/src/main/AndroidManifest.xml && grep -q '@string/admob_app_id' android/app/src/main/AndroidManifest.xml && grep -q 'com.google.android.gms.permission.AD_ID' android/app/src/main/AndroidManifest.xml && grep -q 'ca-app-pub-3940256099942544~3347511713' android/app/src/main/res/values/strings.xml && grep -q 'admob_app_id' android/app/src/main/res/values/strings.xml && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep "com.google.android.gms.ads.APPLICATION_ID" android/app/src/main/AndroidManifest.xml` returns a match
    - `grep "@string/admob_app_id" android/app/src/main/AndroidManifest.xml` returns a match (proves string-resource pattern, not inline)
    - `grep "com.google.android.gms.permission.AD_ID" android/app/src/main/AndroidManifest.xml` returns a match
    - `grep "android.permission.INTERNET" android/app/src/main/AndroidManifest.xml` still returns a match (existing INTERNET not removed)
    - The `<meta-data android:name="com.google.android.gms.ads.APPLICATION_ID"` entry sits between `<application ...>` and its closing `</application>` — NOT inside `<activity>` or `<provider>` (verify by reading the file)
    - `grep "admob_app_id" android/app/src/main/res/values/strings.xml` returns a match
    - `grep "ca-app-pub-3940256099942544~3347511713" android/app/src/main/res/values/strings.xml` returns a match
    - All four pre-existing string resources (`app_name`, `title_activity_main`, `package_name`, `custom_url_scheme`) are still present in strings.xml
    - Both files remain valid XML (balanced tags, single root element)
    - `npm test` exits 0
  </acceptance_criteria>
  <done>Manifest declares AdMob App ID meta-data via @string reference and AD_ID permission for Android 13+; strings.xml contains the test App ID; no existing content removed.</done>
</task>

<task type="auto">
  <name>Task 3: Create .env.development and .env.production with all three AdMob IDs</name>
  <files>.env.development, .env.production</files>
  <read_first>
    - .gitignore (confirm .env.development and .env.production are NOT listed — they must be tracked)
    - .planning/phases/06-android-native-setup/06-CONTEXT.md (D-02 locks exact env file contents)
    - .planning/phases/06-android-native-setup/06-RESEARCH.md (Pattern 3 — Vite mode-based loading)
  </read_first>
  <action>
    1. Create `.env.development` (at project root, NOT inside android/) with this exact content:
       ```ini
       # AdMob test IDs (Google public test values — safe to commit)
       # Loaded by Vite in dev mode (npm run dev / vite build --mode development)
       VITE_ADMOB_APP_ID=ca-app-pub-3940256099942544~3347511713
       VITE_ADMOB_BANNER_ID=ca-app-pub-3940256099942544/6300978111
       VITE_ADMOB_INTERSTITIAL_ID=ca-app-pub-3940256099942544/1033173712
       ```
       All three keys MUST use the `VITE_` prefix (Vite only exposes env vars with that prefix to client code). Values must match D-02 exactly.

    2. Create `.env.production` (at project root) with this exact content:
       ```ini
       # AdMob production IDs — Phase 10 replaces TODO with real values from AdMob console
       # Loaded by Vite in prod mode (npm run build / vite build)
       VITE_ADMOB_APP_ID=TODO
       VITE_ADMOB_BANNER_ID=TODO
       VITE_ADMOB_INTERSTITIAL_ID=TODO
       ```
       Values MUST be the literal string `TODO` (not empty, not placeholder IDs). Phase 10 will replace these with real AdMob console IDs (threat mitigation: T-InfoDisc — no real production IDs committed in Phase 6).

    3. Verify `.gitignore` does NOT contain `.env.development` or `.env.production`. Current .gitignore lists `.env` and `.env.local` only — do NOT add the new files to .gitignore. They are intentionally tracked.

    4. Do NOT edit the existing `.env` file (it holds git-ignored Firebase secrets per RESEARCH Pitfall 6).

    Notes:
    - These are config files, not TypeScript — stays within D-01 (purely native phase boundary).
    - No TypeScript code reads these yet; Phase 7's adService.ts will consume them.
    - `.env.local` takes priority and is git-ignored — developers can override locally if needed.
  </action>
  <verify>
    <automated>test -f .env.development && test -f .env.production && grep -q '^VITE_ADMOB_APP_ID=ca-app-pub-3940256099942544~3347511713$' .env.development && grep -q '^VITE_ADMOB_BANNER_ID=ca-app-pub-3940256099942544/6300978111$' .env.development && grep -q '^VITE_ADMOB_INTERSTITIAL_ID=ca-app-pub-3940256099942544/1033173712$' .env.development && grep -q '^VITE_ADMOB_APP_ID=TODO$' .env.production && grep -q '^VITE_ADMOB_BANNER_ID=TODO$' .env.production && grep -q '^VITE_ADMOB_INTERSTITIAL_ID=TODO$' .env.production && ! grep -q '\.env\.development' .gitignore && ! grep -q '\.env\.production' .gitignore && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `.env.development` exists at the repository root
    - `.env.production` exists at the repository root
    - `grep "^VITE_ADMOB_APP_ID=ca-app-pub-3940256099942544~3347511713$" .env.development` returns a match
    - `grep "^VITE_ADMOB_BANNER_ID=ca-app-pub-3940256099942544/6300978111$" .env.development` returns a match
    - `grep "^VITE_ADMOB_INTERSTITIAL_ID=ca-app-pub-3940256099942544/1033173712$" .env.development` returns a match
    - `grep "^VITE_ADMOB_APP_ID=TODO$" .env.production` returns a match
    - `grep "^VITE_ADMOB_BANNER_ID=TODO$" .env.production` returns a match
    - `grep "^VITE_ADMOB_INTERSTITIAL_ID=TODO$" .env.production` returns a match
    - `.gitignore` does NOT contain `.env.development` or `.env.production` (still only lists `.env` and `.env.local`)
    - Existing `.env` file is unchanged (not modified or overwritten)
    - `git status` shows `.env.development` and `.env.production` as untracked or staged (not ignored)
    - `npm test` exits 0
  </acceptance_criteria>
  <done>Both env files created with Vite-prefixed keys, dev file holds Google test IDs, prod file holds TODO placeholders, .gitignore unchanged, files are git-trackable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time → runtime | Env files baked into Vite bundle; values leave dev machine |
| repo → public git history | Committed env values visible to anyone with repo access |
| app → AdMob SDK | App ID and ad unit IDs determine which account receives revenue |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | `.env.production` committed to repo | mitigate | File contains only `TODO` placeholders — Phase 10 fills real values. Verified by grep for `^VITE_ADMOB_APP_ID=TODO$` in acceptance criteria. Real production IDs never enter Phase 6 history. |
| T-06-02 | Spoofing / Policy violation | AdMob App ID in `strings.xml` | mitigate | Phase 6 uses only Google's public test App ID `ca-app-pub-3940256099942544~3347511713`. Using a real App ID during development violates AdMob policy and risks account suspension. Verified by exact string match in acceptance criteria. |
| T-06-03 | Tampering | Firebase/AdMob manifest merge conflict at build time | mitigate | Pin `playServicesAdsVersion = '24.3.0'` in `android/variables.gradle` overriding plugin default `24.9.+` — prevents `ProcessLibraryManifest` error per RESEARCH Pitfall 3. Fallback `tools:replace` available if pin alone insufficient (documented in Pitfall 3 for Plan 02 checkpoint to apply if build fails). |
| T-06-04 | Information Disclosure | Accidental edit to base `.env` (Firebase secrets) | mitigate | Task 3 action explicitly forbids editing `.env`. AdMob IDs go exclusively to `.env.development` and `.env.production`. `.env` remains git-ignored and unchanged. |
| T-06-05 | Elevation of Privilege | Android 13+ advertising ID returning zeros (no AD_ID permission) | mitigate | Task 2 adds `<uses-permission android:name="com.google.android.gms.permission.AD_ID"/>` — required by API 33+ (project targetSdkVersion=36). Plugin does NOT auto-include this per RESEARCH finding. |
| T-06-06 | Repudiation | Real production IDs leaked into version control | accept | Phase 6 uses only public test IDs. Phase 10 carries the real-ID leak risk; handled by that phase's threat model (committed to `.env.production` only at release time, reviewed via diff). |
</threat_model>

<verification>
After all 3 tasks complete:

1. `npm test` exits 0 (existing Vitest suite — no TypeScript regressions)
2. `npm ls @capacitor-community/admob` shows `8.0.0` (no range)
3. `grep playServicesAdsVersion android/variables.gradle` returns `playServicesAdsVersion = '24.3.0'`
4. `grep APPLICATION_ID android/app/src/main/AndroidManifest.xml` returns the meta-data line
5. `grep AD_ID android/app/src/main/AndroidManifest.xml` returns the permission line
6. `grep admob_app_id android/app/src/main/res/values/strings.xml` returns the resource
7. `cat .env.development .env.production` shows both files with the expected contents
8. `git status` shows `.env.development` and `.env.production` as new/untracked (not ignored)
9. `grep capacitor-community-admob android/capacitor.settings.gradle` returns a match (cap sync was run)

Device-level verification (launch, adb dumpsys) is deferred to Plan 02 (manual checkpoint).
</verification>

<success_criteria>
- Plugin `@capacitor-community/admob@8.0.0` installed as an exact dependency (SETUP-01)
- `npx cap sync` ran successfully and registered the Android subproject (SETUP-01)
- `playServicesAdsVersion = '24.3.0'` pinned in variables.gradle (SETUP-03)
- AdMob App ID meta-data present under `<application>` via `@string/admob_app_id` reference (SETUP-02)
- `admob_app_id` string resource in strings.xml equals the Google public test App ID (SETUP-02)
- `AD_ID` permission declared in manifest for Android 13+ compatibility (SETUP-04)
- `.env.development` contains all three `VITE_ADMOB_*` keys with Google test values (SETUP-05)
- `.env.production` contains all three `VITE_ADMOB_*` keys with `TODO` placeholders (SETUP-05)
- Existing Vitest suite still green (no regression)
- No TypeScript files created or modified (honors D-01)
</success_criteria>

<output>
After completion, create `.planning/phases/06-android-native-setup/06-01-SUMMARY.md` documenting:
- Files changed with exact diffs
- Output of `npx cap sync` (plugin registration line)
- Any deviations from planned values (e.g., if npm added a caret that was manually removed)
- Confirmation that no TypeScript files were touched
- Hand-off note: Plan 02 must run before phase completion to perform manual Android build + launch verification
</output>
